package com.suihan74.utilities

/** (外部から)コンテンツを一番上までスクロールできる */
interface ScrollableToTop {
    fun scrollToTop()
}

/** (外部から)コンテンツを一番下までスクロールできる */
interface ScrollableToBottom {
    fun scrollToBottom()
}
